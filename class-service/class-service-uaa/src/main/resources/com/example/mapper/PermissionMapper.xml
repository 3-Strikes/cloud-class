<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.PermissionMapper">

    <select id="selectPerms" resultType="com.example.domain.Permission">
        select p.* from t_login_role r left join t_role_permission rp on r.role_id=rp.role_id left join t_permission p on rp.permission_id=p.id where r.login_id=#{loginId}
    </select>
</mapper>
